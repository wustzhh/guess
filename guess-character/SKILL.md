---
name: guess-character
description: 猜人物小游戏。Agent 暗选人物，用户通过提问猜出身份。支持 Web 服务端（Flask+DeepSeek API+SQLite）和纯 CLI 两种模式。Use when user says "猜人物", "猜角色", "guess character", "玩猜人游戏", or wants to play a character-guessing game.
---

# 猜人物游戏

## 游戏规则

1. **Agent 选角色**：从用户指定范围中暗选一个人物，不透露任何信息
2. **用户提问**：通过提问逐步缩小范围
3. **Agent 回答限制**：只能以下列格式回答，**绝不在回答中追加任何解释或暗示**：

| 问题类型 | 回答格式 | 示例 |
|----------|---------|------|
| 是非题，能明确答 | `是` 或 `不是`（就一个字） | "是蜀国的吗" → `是` |
| 二选一题（含"还是"） | 直接给选项词，不套"是也不是" | "死于234年之前还是之后" → `234年之前死的` |
| 模棱两可，无法简单二分 | `是也不是，` + 泛词（≤6字） | "偏文还是偏武" → `是也不是，偏文` |
| 确实无法判断 | `无法判断，换种方式问吧` | 事件同年发生先后不明 |

4. **铁律**：
   - ⛔ "是"/"不是"后面绝不许跟任何字——就那一个字
   - ⛔ "是也不是"的解释绝不许带身份标签（如"群雄""诸侯""被讨伐""权臣"）
   - ⛔ 绝不许提名字/字号/绰号/典故/代表事件
   - ⛔ 绝不许评价人物（如"很出名""不怎么样"）
   - ⛔ 事件时序不确定时，必须说"无法判断"，不可瞎猜
   - ⛔ "主要活在XX一朝"类问题必须严格按实际寿命时长判断，不能因为人物在某朝功业最盛就答"是"。如李靖71%人生在隋朝，却因盛名在唐而错答"是"，应严格比较年代占比

5. **猜中后科普**：输出人物生平简介
6. **战绩汇总**：每局结束输出战绩表 + 评价

## 游戏流程

### 第一步：选角入库

用户指定范围后，选一个**有知名度但不是最热门**的角色。

**必须**先将角色写入DB，确保预选而非临时筛选：

```bash
python D:\pyProj\guess\game_db.py new-game "<分类>" "<角色名>"
```

回复：`好，我已经想好了一个{范围}的人物。开始提问吧！`

### 第二步：回答提问

每轮提问时，先从DB读取当前角色再作答：

```bash
python D:\pyProj\guess\game_db.py current
```

根据读取到的角色名和自身知识作答。严格遵守格式表。常见提问维度（不主动提示）：
- 时间线 / 阵营 / 身份类型 / 关键事件 / 人物关系

### 第三步：猜中或放弃

**猜中** → 科普人物生平（姓名字号生卒年、出身、主要事迹、结局、一句话评价）

**放弃** → 公布答案 + 同样科普

### 第四步：战绩汇总

```
## 猜人战绩单

| 轮次 | 你的提问 | 我的回答 |
|------|---------|---------|
| 1    | xxx？   | 是       |
| ...  | ...     | ...     |
| N    | **xxx！**| **正确！**|

**总计：X次提问 + 1次猜测 = Y轮猜出答案**

评价：{趣味评价}
```

趣味评价：
- ≤4轮：神机妙算！
- 5-7轮：思路清晰！
- 8-10轮：稳扎稳打
- 11-15轮：锲而不舍
- 16+轮：终得正果
- 放弃：虽败犹荣

### 第五步：保存战绩（必须）

**重要**：JSON 通过临时文件传入以避开 PowerShell 编码问题。

先将日志写入临时文件，再调用 save 命令：

```bash
python -c "import json; open(r'C:\Users\ADMINI~1\AppData\Local\Temp\opencode\game_log.json','w',encoding='utf-8').write(json.dumps(日志数据, ensure_ascii=False))"
python D:\pyProj\guess\game_db.py save "<分类>" "<角色名>" <提问数> <总轮次> <结果> --log-file C:\Users\ADMINI~1\AppData\Local\Temp\opencode\game_log.json
```

保存后告知：**战绩已保存！打开 `stats.html` 可查看。**

### 禁止的回答示例

```
❌ "不是，他是被讨伐的对象"
❌ "不是，是群雄阵营的"
❌ "是也不是，割据一方的诸侯"
❌ "是也不是，反叛了朝廷"
✅ "不是"
✅ "偏文"
✅ "234年之前死的"
```

## 服务端部署

项目支持 Web 端运行（Flask + SQLite），支持登录和多账号独立战绩。

```bash
# 安装
pip install flask

# 注册用户
python server/app.py --create-user 用户名 密码

# 启动（接入 DeepSeek API 以获得更智能的回答）
python server/app.py --api-key sk-xxx --host 0.0.0.0 --port 5000

# 不接 API 则使用本地规则匹配
python server/app.py --host 0.0.0.0 --port 5000
```

项目文件结构见 `README.md`。
